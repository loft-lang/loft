diff --git b/default/01_code.loft a/default/01_code.loft
index 01a818d9d..a5a9d842b 100644
--- b/default/01_code.loft
+++ a/default/01_code.loft
@@ -220,8 +220,8 @@ fn OpCastFloatFromText(v1: text) -> float;
 fn OpAbsInt(v1: integer) -> integer;
 #rust"ops::op_abs_int(@v1)"
 // Absolute value. Removes the sign from a negative integer.
-pub fn abs(self: integer) -> integer {
-  OpAbsInt(self)
+pub fn abs(both: integer) -> integer {
+  OpAbsInt(both)
 }
 #pure
 
@@ -306,8 +306,8 @@ fn OpFormatStackInt(
 // `floor_mod` wraps (`(-1).floor_mod(3) == 2`), which is what circular indexing
 // / wrap-around wants (`grid[(i - 1).floor_mod(w)]`).  `x.floor_mod(0)` is null,
 // like `%` (C80 — an undefined value is a null, never a fault).
-pub fn floor_mod(self: integer, divisor: integer) -> integer? {
-  ((self % divisor)+divisor) % divisor
+pub fn floor_mod(both: integer, divisor: integer) -> integer? {
+  ((both % divisor)+divisor) % divisor
 }
 #pure
 // Single-precision (32-bit) floating-point math.
@@ -323,8 +323,8 @@ fn OpConvSingleFromNull() -> single;
 fn OpAbsSingle(v1: single) -> single;
 #rust"@v1.abs()"
 // Absolute value for single-precision floats.
-pub fn abs(self: single) -> single {
-  OpAbsSingle(self)
+pub fn abs(both: single) -> single {
+  OpAbsSingle(both)
 }
 #pure
 
@@ -361,64 +361,64 @@ fn OpRemSingleNullable(v1: single, v2: single) -> single;
 fn OpMathFuncSingle(fn_id: const i8, v1: single) -> single;
 #rust"match @fn_id {{ 0 => @v1.cos(), 1 => @v1.sin(), 2 => @v1.tan(), 3 => @v1.acos(), 4 => @v1.asin(), 5 => @v1.atan(), 6 => @v1.ceil(), 7 => @v1.floor(), 8 => @v1.round(), 9 => @v1.sqrt(), _ => f32::NAN }}"
 // Cosine. Use for circular motion: x = r * cos(angle).
-pub fn cos(self: single) -> single {
-  OpMathFuncSingle(0, self)
+pub fn cos(both: single) -> single {
+  OpMathFuncSingle(0, both)
 }
 #pure
 // Sine. Use for circular motion: y = r * sin(angle).
 
-pub fn sin(self: single) -> single {
-  OpMathFuncSingle(1, self)
+pub fn sin(both: single) -> single {
+  OpMathFuncSingle(1, both)
 }
 #pure
 // Tangent. Use for slopes and perspective projection.
 
-pub fn tan(self: single) -> single {
-  OpMathFuncSingle(2, self)
+pub fn tan(both: single) -> single {
+  OpMathFuncSingle(2, both)
 }
 #pure
 // Arc cosine. Returns the angle (in radians) whose cosine is v.
 
-pub fn acos(self: single) -> single? {
-  OpMathFuncSingle(3, self)
+pub fn acos(both: single) -> single? {
+  OpMathFuncSingle(3, both)
 }
 #pure
 // Arc sine. Returns the angle whose sine is v.
 
-pub fn asin(self: single) -> single? {
-  OpMathFuncSingle(4, self)
+pub fn asin(both: single) -> single? {
+  OpMathFuncSingle(4, both)
 }
 #pure
 // Arc tangent of a single value. Returns angle in (-PI/2, PI/2).
 
-pub fn atan(self: single) -> single {
-  OpMathFuncSingle(5, self)
+pub fn atan(both: single) -> single {
+  OpMathFuncSingle(5, both)
 }
 #pure
 // Round up to the nearest integer value.
 // Use to compute required buffer sizes from fractional counts.
 
-pub fn ceil(self: single) -> single {
-  OpMathFuncSingle(6, self)
+pub fn ceil(both: single) -> single {
+  OpMathFuncSingle(6, both)
 }
 #pure
 // Round down to the nearest integer value.
 // Use to convert a float position to a tile index.
 
-pub fn floor(self: single) -> single {
-  OpMathFuncSingle(7, self)
+pub fn floor(both: single) -> single {
+  OpMathFuncSingle(7, both)
 }
 #pure
 // Round to the nearest integer value (half rounds away from zero).
 
-pub fn round(self: single) -> single {
-  OpMathFuncSingle(8, self)
+pub fn round(both: single) -> single {
+  OpMathFuncSingle(8, both)
 }
 #pure
 // Square root. Use for distances and normalization.
 
-pub fn sqrt(self: single) -> single? {
-  OpMathFuncSingle(9, self)
+pub fn sqrt(both: single) -> single? {
+  OpMathFuncSingle(9, both)
 }
 #pure
 
@@ -426,15 +426,15 @@ fn OpMathFunc2Single(fn_id: const i8, v1: single, v2: single) -> single;
 #rust"match @fn_id {{ 0 => @v1.atan2(@v2), 1 => @v1.log(@v2), _ => f32::NAN }}"
 // Arc tangent of y/x, preserving the correct quadrant.
 // Use instead of atan when you have separate x/y components.
-pub fn atan2(self: single, v2: single) -> single {
-  OpMathFunc2Single(0, self, v2)
+pub fn atan2(both: single, v2: single) -> single {
+  OpMathFunc2Single(0, both, v2)
 }
 #pure
 // Logarithm of v in the given base.
 // Use for converting between scales (e.g., decibels).
 
-pub fn log(self: single, v2: single) -> single? {
-  OpMathFunc2Single(1, self, v2)
+pub fn log(both: single, v2: single) -> single? {
+  OpMathFunc2Single(1, both, v2)
 }
 #pure
 
@@ -442,8 +442,8 @@ fn OpPowSingle(v1: single, v2: single) -> single;
 #rust"@v1.powf(@v2)"
 // Raises base to the power exp.
 // Use for exponential growth curves and scaling.
-pub fn pow(self: single, v2: single) -> single? {
-  OpPowSingle(self, v2)
+pub fn pow(both: single, v2: single) -> single? {
+  OpPowSingle(both, v2)
 }
 #pure
 
@@ -478,8 +478,8 @@ fn OpConvFloatFromNull() -> float;
 fn OpAbsFloat(v1: float) -> float;
 #rust"@v1.abs()"
 // Absolute value for double-precision floats.
-pub fn abs(self: float) -> float {
-  OpAbsFloat(self)
+pub fn abs(both: float) -> float {
+  OpAbsFloat(both)
 }
 #pure
 
@@ -494,64 +494,64 @@ pub E = OpMathEFloat();
 fn OpMathFuncFloat(fn_id: const i8, v1: float) -> float;
 #rust"match @fn_id {{ 0 => @v1.cos(), 1 => @v1.sin(), 2 => @v1.tan(), 3 => @v1.acos(), 4 => @v1.asin(), 5 => @v1.atan(), 6 => @v1.ceil(), 7 => @v1.floor(), 8 => @v1.round(), 9 => @v1.sqrt(), _ => f64::NAN }}"
 // Cosine. Use for circular motion: x = r * cos(angle).
-pub fn cos(self: float) -> float {
-  OpMathFuncFloat(0, self)
+pub fn cos(both: float) -> float {
+  OpMathFuncFloat(0, both)
 }
 #pure
 // Sine. Use for circular motion: y = r * sin(angle).
 
-pub fn sin(self: float) -> float {
-  OpMathFuncFloat(1, self)
+pub fn sin(both: float) -> float {
+  OpMathFuncFloat(1, both)
 }
 #pure
 // Tangent. Use for slopes and perspective projection.
 
-pub fn tan(self: float) -> float {
-  OpMathFuncFloat(2, self)
+pub fn tan(both: float) -> float {
+  OpMathFuncFloat(2, both)
 }
 #pure
 // Arc cosine. Returns the angle (in radians) whose cosine is v.
 
-pub fn acos(self: float) -> float? {
-  OpMathFuncFloat(3, self)
+pub fn acos(both: float) -> float? {
+  OpMathFuncFloat(3, both)
 }
 #pure
 // Arc sine. Returns the angle whose sine is v.
 
-pub fn asin(self: float) -> float? {
-  OpMathFuncFloat(4, self)
+pub fn asin(both: float) -> float? {
+  OpMathFuncFloat(4, both)
 }
 #pure
 // Arc tangent of a single value. Returns angle in (-PI/2, PI/2).
 
-pub fn atan(self: float) -> float {
-  OpMathFuncFloat(5, self)
+pub fn atan(both: float) -> float {
+  OpMathFuncFloat(5, both)
 }
 #pure
 // Round up to the nearest integer value.
 // Use to compute required buffer sizes from fractional counts.
 
-pub fn ceil(self: float) -> float {
-  OpMathFuncFloat(6, self)
+pub fn ceil(both: float) -> float {
+  OpMathFuncFloat(6, both)
 }
 #pure
 // Round down to the nearest integer value.
 // Use to convert a float position to a tile index.
 
-pub fn floor(self: float) -> float {
-  OpMathFuncFloat(7, self)
+pub fn floor(both: float) -> float {
+  OpMathFuncFloat(7, both)
 }
 #pure
 // Round to the nearest integer value (half rounds away from zero).
 
-pub fn round(self: float) -> float {
-  OpMathFuncFloat(8, self)
+pub fn round(both: float) -> float {
+  OpMathFuncFloat(8, both)
 }
 #pure
 // Square root. Use for distances and normalization.
 
-pub fn sqrt(self: float) -> float? {
-  OpMathFuncFloat(9, self)
+pub fn sqrt(both: float) -> float? {
+  OpMathFuncFloat(9, both)
 }
 #pure
 
@@ -559,15 +559,15 @@ fn OpMathFunc2Float(fn_id: const i8, v1: float, v2: float) -> float;
 #rust"match @fn_id {{ 0 => @v1.atan2(@v2), 1 => @v1.log(@v2), _ => f64::NAN }}"
 // Arc tangent of y/x, preserving the correct quadrant.
 // Use instead of atan when you have separate x/y components.
-pub fn atan2(self: float, v2: float) -> float {
-  OpMathFunc2Float(0, self, v2)
+pub fn atan2(both: float, v2: float) -> float {
+  OpMathFunc2Float(0, both, v2)
 }
 #pure
 // Logarithm of v in the given base.
 // Use for converting between scales (e.g., decibels).
 
-pub fn log(self: float, v2: float) -> float? {
-  OpMathFunc2Float(1, self, v2)
+pub fn log(both: float, v2: float) -> float? {
+  OpMathFunc2Float(1, both, v2)
 }
 #pure
 
@@ -575,57 +575,57 @@ fn OpPowFloat(v1: float, v2: float) -> float;
 #rust"@v1.powf(@v2)"
 // Raises base to the power exp.
 // Use for exponential growth curves and scaling.
-pub fn pow(self: float, v2: float) -> float? {
-  OpPowFloat(self, v2)
+pub fn pow(both: float, v2: float) -> float? {
+  OpPowFloat(both, v2)
 }
 #pure
 // --- exp / ln / log2 / log10 ---
 // Raises E (2.71828…) to the power v. Use for exponential growth models.
 
-pub fn exp(self: single) -> single {
-  OpPowSingle(E as single, self)
+pub fn exp(both: single) -> single {
+  OpPowSingle(E as single, both)
 }
 #pure
 
 // Double-precision exp (e^v).
-pub fn exp(self: float) -> float {
-  OpPowFloat(E, self)
+pub fn exp(both: float) -> float {
+  OpPowFloat(E, both)
 }
 #pure
 // Natural logarithm (base E). Use for growth rates and information entropy.
 
-pub fn ln(self: single) -> single? {
-  log(self, E as single)
+pub fn ln(both: single) -> single? {
+  log(both, E as single)
 }
 #pure
 
 // Double-precision natural logarithm.
-pub fn ln(self: float) -> float? {
-  log(self, E)
+pub fn ln(both: float) -> float? {
+  log(both, E)
 }
 #pure
 // Base-2 logarithm. Use for bit-count calculations and information theory.
 
-pub fn log2(self: single) -> single? {
-  log(self, 2.0f)
+pub fn log2(both: single) -> single? {
+  log(both, 2.0f)
 }
 #pure
 
 // Double-precision base-2 logarithm.
-pub fn log2(self: float) -> float? {
-  log(self, 2.0)
+pub fn log2(both: float) -> float? {
+  log(both, 2.0)
 }
 #pure
 // Base-10 logarithm. Use for decibels, orders of magnitude, and display scales.
 
-pub fn log10(self: single) -> single? {
-  log(self, 10.0f)
+pub fn log10(both: single) -> single? {
+  log(both, 10.0f)
 }
 #pure
 
 // Double-precision base-10 logarithm.
-pub fn log10(self: float) -> float? {
-  log(self, 10.0)
+pub fn log10(both: float) -> float? {
+  log(both, 10.0)
 }
 #pure
 
@@ -685,62 +685,62 @@ fn OpFormatStackFloat(pos: const u16, val: float, width: integer, precision: int
 // propagates: the answer is null if either argument is null.  Otherwise you get the
 // non-null overload and a non-null answer, so a plain `min(a, b)` needs no discharge.
 // Smallest of two integer values.
-pub fn min(self: integer, b: integer) -> integer {
-  if self <= b { self } else { b }
+pub fn min(both: integer, b: integer) -> integer {
+  if both <= b { both } else { b }
 }
 #pure
 // Largest of two integer values.
-pub fn max(self: integer, b: integer) -> integer {
-  if self >= b { self } else { b }
+pub fn max(both: integer, b: integer) -> integer {
+  if both >= b { both } else { b }
 }
 #pure
 // Clamps v into the inclusive range [lo, hi]. Returns null if any argument is null.
-pub fn clamp(self: integer, lo: integer, hi: integer) -> integer {
-  min(max(self, lo), hi)
+pub fn clamp(both: integer, lo: integer, hi: integer) -> integer {
+  min(max(both, lo), hi)
 }
 #pure
 
 // Smallest of two single-precision float values.
-pub fn min(self: single, b: single) -> single {
-  if self <= b { self } else { b }
+pub fn min(both: single, b: single) -> single {
+  if both <= b { both } else { b }
 }
 #pure
 // Largest of two single-precision float values.
-pub fn max(self: single, b: single) -> single {
-  if self >= b { self } else { b }
+pub fn max(both: single, b: single) -> single {
+  if both >= b { both } else { b }
 }
 #pure
 // Clamps v into the inclusive range [lo, hi]. Returns null if any argument is null.
-pub fn clamp(self: single, lo: single, hi: single) -> single {
-  min(max(self, lo), hi)
+pub fn clamp(both: single, lo: single, hi: single) -> single {
+  min(max(both, lo), hi)
 }
 #pure
 // Smallest of two double-precision float values.
-pub fn min(self: float, b: float) -> float {
-  if self <= b { self } else { b }
+pub fn min(both: float, b: float) -> float {
+  if both <= b { both } else { b }
 }
 #pure
 // Largest of two double-precision float values.
-pub fn max(self: float, b: float) -> float {
-  if self >= b { self } else { b }
+pub fn max(both: float, b: float) -> float {
+  if both >= b { both } else { b }
 }
 #pure
 // Clamps v into the inclusive range [lo, hi]. Returns null if any argument is null.
-pub fn clamp(self: float, lo: float, hi: float) -> float {
-  min(max(self, lo), hi)
+pub fn clamp(both: float, lo: float, hi: float) -> float {
+  min(max(both, lo), hi)
 }
 #pure
 // Approximate equality: true when a and b differ by at most eps (inclusive).
 // `==` on float/single is EXACT IEEE equality — use `approx` when you want a
 // tolerance, e.g. comparing computed transcendentals: `approx(sqrt(2.0) * sqrt(2.0), 2.0, 1e-9)`.
 // A null (NaN) operand is not approximately equal to anything, so the result is false.
-pub fn approx(self: float, b: float, eps: float) -> boolean {
-  if !self || !b { false } else { abs(self - b) <= eps }
+pub fn approx(both: float, b: float, eps: float) -> boolean {
+  if !both || !b { false } else { abs(both - b) <= eps }
 }
 #pure
 // Approximate equality for single-precision floats (see the float overload).
-pub fn approx(self: single, b: single, eps: single) -> boolean {
-  if !self || !b { false } else { abs(self - b) <= eps }
+pub fn approx(both: single, b: single, eps: single) -> boolean {
+  if !both || !b { false } else { abs(both - b) <= eps }
 }
 #pure
 // --- Text ---
@@ -760,8 +760,8 @@ fn OpLengthText(v1: text) -> integer;
 // Number of characters (Unicode code points) in the text — the human
 // count, as in every mainstream language. For a byte length (bounds checks, the
 // limit of byte-positioned indexing / slicing) use `size`.
-pub fn len(self: text) -> integer {
-  OpLengthText(self)
+pub fn len(both: text) -> integer {
+  OpLengthText(both)
 }
 #pure
 
@@ -770,15 +770,15 @@ fn OpSizeText(v1: text) -> integer;
 // Number of bytes in the text. This is the bound for byte-positioned
 // operations: `s[i]`, slices `s[a..b]`, and `find`/`rfind` all use byte offsets.
 // For the human character count use `len`.
-pub fn size(self: text) -> integer {
-  OpSizeText(self)
+pub fn size(both: text) -> integer {
+  OpSizeText(both)
 }
 #pure
 
 fn OpLengthCharacter(v1: character) -> integer;
 // Byte length of the character's UTF-8 encoding (1–4).
-pub fn len(self: character) -> integer {
-  OpLengthCharacter(self)
+pub fn len(both: character) -> integer {
+  OpLengthCharacter(both)
 }
 #pure
 
@@ -1106,8 +1106,8 @@ fn OpDistinctStore(a: reference, b: reference) -> boolean;
 fn OpRefAlias(r: reference) -> reference;
 #rust"@r"
 // Number of elements in the vector. Use in loop bounds: for i in 0..v.len().
-pub fn len(self: vector) -> integer {
-  OpLengthVector(self)
+pub fn len(both: vector) -> integer {
+  OpLengthVector(both)
 }
 #pure
 
@@ -1144,16 +1144,16 @@ fn OpSizeScalar(v: integer, sz: const u16) -> integer;
 fn OpLengthSorted(r: sorted) -> integer;
 #rust"i64::from(vector::length_vector(&@r, &s.database.allocations))"
 // Number of elements in a sorted collection.
-pub fn len(self: sorted) -> integer {
-  OpLengthSorted(self)
+pub fn len(both: sorted) -> integer {
+  OpLengthSorted(both)
 }
 #pure
 
 fn OpClearVector(r: vector);
 #rust"s.database.clear_vector_release(&@r);"
 // Remove all elements from the vector, setting its length to 0.
-pub fn clear(self: vector) {
-  OpClearVector(self)
+pub fn clear(both: vector) {
+  OpClearVector(both)
 }
 #impure(parent_write)
 
@@ -1350,8 +1350,8 @@ type index size(4);
 fn OpLengthHash(r: hash) -> integer;
 #rust"i64::from(hash::count(&@r, &s.database.allocations))"
 // Number of elements in a hash collection.
-pub fn len(self: hash) -> integer {
-  OpLengthHash(self)
+pub fn len(both: hash) -> integer {
+  OpLengthHash(both)
 }
 #pure
 
@@ -1367,8 +1367,8 @@ fn OpSizeHash(r: hash) -> integer;
 // addressing's spare capacity IS the format).  Allocation-local: the entry
 // records live in separate allocations and are not counted.  Grows in steps as
 // the table rehashes (load factor 0.75).  0 for an empty (unallocated) hash.
-pub fn size(self: hash) -> integer {
-  OpSizeHash(self)
+pub fn size(both: hash) -> integer {
+  OpSizeHash(both)
 }
 #pure
 
@@ -1528,8 +1528,8 @@ type trie;
 fn OpLengthSpatial(r: spatial) -> integer;
 #rust"i64::from(codegen_runtime::spatial_len(&@r, &s.database.allocations))"
 // Number of elements in a spatial (radix / Morton tree) collection.
-pub fn len(self: spatial) -> integer {
-  OpLengthSpatial(self)
+pub fn len(both: spatial) -> integer {
+  OpLengthSpatial(both)
 }
 #pure
 // Number of elements in a trie.  Same cached length word as `len(spatial)` —
@@ -1537,8 +1537,8 @@ pub fn len(self: spatial) -> integer {
 fn OpLengthTrie(r: trie) -> integer;
 #rust"i64::from(codegen_runtime::trie_len(&@r, &s.database.allocations))"
 // Number of elements in the trie.
-pub fn len(self: trie) -> integer {
-  OpLengthTrie(self)
+pub fn len(both: trie) -> integer {
+  OpLengthTrie(both)
 }
 #pure
 // Call a predefined system function (but not an operator).
diff --git b/default/02_files.loft a/default/02_files.loft
index 724017afd..365e365e7 100644
--- b/default/02_files.loft
+++ a/default/02_files.loft
@@ -268,8 +268,8 @@ pub fn exists(path: text) -> boolean fs#read {
 #impure(io)
 // Method form: f = file("path"); if f.exists() { .. }
 // Also callable as exists(file_obj) via the 'both' parameter name.
-pub fn exists(self: File) -> boolean fs#read {
-  self.format != Format.NotExists
+pub fn exists(both: File) -> boolean fs#read {
+  both.format != Format.NotExists
 }
 #pure
 // Use to remove a file after processing or as a cleanup step.
diff --git b/default/03_text.loft a/default/03_text.loft
index 3dc211060..7f08241b8 100644
--- b/default/03_text.loft
+++ a/default/03_text.loft
@@ -67,8 +67,8 @@ pub fn char_slice(self: text, from: integer, to: integer) -> text {
 // `02_files.loft` § Path helpers, so they're available before
 // `03_text.loft` loads — same call shape, same `pub fn` signatures.)
 // Removes leading and trailing whitespace. Use when processing user input or file content.
-pub fn trim(self: text) -> text[self];
-#rust"@self.trim()"
+pub fn trim(both: text) -> text[both];
+#rust"@both.trim()"
 #pure
 // Removes leading whitespace only.
 pub fn trim_start(self: text) -> text[self];
diff --git b/src/data.rs a/src/data.rs
index d4c0ca5e0..c26a6d655 100644
--- b/src/data.rs
+++ a/src/data.rs
@@ -7591,8 +7591,8 @@ impl Data {
                 Level::Error,
                 "Cannot redefine '{bare}' (already defined at {}) — a name has one body per \
                  receiver type, and `x.{bare}(…)` and `{bare}(x, …)` would reach different \
-                 functions; declare it once as a `self` method, which takes both spellings, or \
-                 rename one",
+                 functions; declare it once with its first parameter named `both` to give that \
+                 body both spellings, or rename one",
                 self.def(other).position
             );
         } else if o_nr != u32::MAX
@@ -7852,11 +7852,7 @@ impl Data {
                 self.definitions[type_nr as usize].attributes[a_nr].constant = true;
             }
         }
-        // A `self` method registers under its bare name too, exactly as `both` always did: the
-        // two call spellings already resolve identically ((F-Recv)), and the bare-name
-        // dispatcher is what an import list finds, so `use lib::(twice)` reaches a `self`
-        // method the way it reached a `both` one (C123 — `both` is deprecated).
-        if is_self || is_both || overload_label.is_some() {
+        if is_both || overload_label.is_some() {
             let mut main = self.def_nr(fn_name);
             if main == u32::MAX {
                 main = self.add_def(fn_name, lexer.pos(), DefType::Dynamic);
diff --git b/src/parser/definitions.rs a/src/parser/definitions.rs
index 9bb9a702b..e7fecb1bd 100644
--- b/src/parser/definitions.rs
+++ a/src/parser/definitions.rs
@@ -1800,20 +1800,6 @@ impl Parser {
                  {type_var_name})` and call it as `{fn_name}(x)`"
             );
         }
-        // C123 — `both` as a first parameter is deprecated: `self` already gives a function both
-        // call spellings, so the two names meant one thing.  Still accepted, meaning exactly
-        // `self`, and warned — a warning gates a library's CI, which is how published uses are
-        // found and renamed.
-        if !self.first_pass && arguments.first().is_some_and(|a| a.name == "both") {
-            diagnostic!(
-                self.lexer,
-                Level::Warning,
-                code = "both-receiver-deprecated",
-                "`both` as a first parameter is deprecated — `self` already gives `{fn_name}` \
-                 both call spellings, `x.{fn_name}(…)` and `{fn_name}(x, …)`; rename the \
-                 parameter to `self`"
-            );
-        }
         // @PLN102 Phase 3 (N-Domain) — the domain-partial math fns are declared `-> τ?` in the
         // stdlib (they yield the reserved null out of their real domain). When LOFT_NULLFLOW is
         // OFF, strip the `?` so their return stays non-null and the default surface is byte-
diff --git b/src/parser/mod.rs a/src/parser/mod.rs
index bb58239ad..a085c1398 100644
--- b/src/parser/mod.rs
+++ a/src/parser/mod.rs
@@ -6519,14 +6519,7 @@ impl Parser {
                     let declared_by = if from_stdlib {
                         format!("stdlib declared `{name}` as a method")
                     } else {
-                        // A method answers the bare spelling too ((F-Recv)) — for a receiver of
-                        // its type, once the name is in scope — so "not a free function" was
-                        // wrong for every method, and pointed away from the import it needed.
-                        format!(
-                            "`{name}` is a method: `{name}(x, …)` reaches it for a receiver of \
-                             that type once the name is in scope — a package's method is \
-                             imported by name, `use <package>::({name})`"
-                        )
+                        format!("`{name}` is declared as a method, not as a free function")
                     };
                     diagnostic_at!(
                         self.lexer,
@@ -12966,7 +12959,7 @@ impl Parser {
                             "`{nm}` is a method on `{on}`, and a method is not a function VALUE \
                              — there is nothing to pass here. Wrap it: `|x| {{ x.{nm}(…) }}`, or \
                              declare the function with a plain first-parameter name (not \
-                             `self`), which makes it a free function and a usable fn-ref"
+                             `self` / `both`), which makes it a free function and a usable fn-ref"
                         );
                     } else {
                         self.validate_convert(&context, actual_type, &tp, &pos);
diff --git b/src/parser/objects.rs a/src/parser/objects.rs
index b8d7b5321..410816dc3 100644
--- b/src/parser/objects.rs
+++ a/src/parser/objects.rs
@@ -808,7 +808,8 @@ impl Parser {
                             "`{name}` is a method on `{on}`, and a method is not a function \
                              VALUE — there is nothing to bind here. Wrap it: `|x| {{ x.{name}(…) \
                              }}`, or declare the function with a plain first-parameter name \
-                             (not `self`), which makes it a free function and a usable fn-ref"
+                             (not `self` / `both`), which makes it a free function and a usable \
+                             fn-ref"
                         );
                     } else if let Some(s) = suggestion {
                         diagnostic_at!(
@@ -834,10 +835,10 @@ impl Parser {
                     t = Type::Never;
                 }
             } else {
-                // loft#1008 — the OTHER half. A `self` receiver (and the deprecated `both`,
-                // C123) registers a dispatch entry under the PLAIN name — what an import list
-                // finds — so the bare name is FOUND here, as a `Dynamic` def, and fell through
-                // to a silent null. `x = f` bound null with
+                // loft#1008 — the OTHER half. A `both` receiver registers a dispatch entry
+                // under the PLAIN name (which is what makes the free-call spelling `f(x)`
+                // work), so unlike a `self` method the bare name is FOUND here — as a
+                // `Dynamic` def — and fell through to a silent null. `x = f` bound null with
                 // no diagnostic at all, and the error surfaced later as whatever used it
                 // ("Cannot format type null"); in a fn-ref argument it reached the call check
                 // as a bare `Value::Null` with no name attached, reported as *"expected
@@ -853,24 +854,15 @@ impl Parser {
                     if !receivers.is_empty() {
                         reported_method = true;
                         let on = receivers.join("`, `");
-                        // A fn-typed PARAMETER seeds `expected` for its argument (`parse_call`),
-                        // so there the name is being passed; anywhere else it is being bound.
-                        // A `self` method reaches this branch too since it registers its bare
-                        // name (C123), and the argument site's wording is the one it had.
-                        let verb = if matches!(self.expected.base(), Type::Function(_, _, _)) {
-                            "pass"
-                        } else {
-                            "bind"
-                        };
                         diagnostic_at!(
                             self.lexer,
                             name_pos,
                             Level::Error,
                             "`{name}` is a method on `{on}`, and a method is not a function \
-                             VALUE — there is nothing to {verb} here. Wrap it: `|x| {{ \
-                             x.{name}(…) }}`, or declare the function with a plain \
-                             first-parameter name (not `self`), which makes it a free function \
-                             and a usable fn-ref"
+                             VALUE — there is nothing to bind here. Wrap it: `|x| {{ x.{name}(…) \
+                             }}`, or declare the function with a plain first-parameter name \
+                             (not `self` / `both`), which makes it a free function and a usable \
+                             fn-ref"
                         );
                     }
                 }
@@ -1041,8 +1033,8 @@ impl Parser {
                                 "`{name}` is a method on `{on}`, and a method is not a function \
                                  VALUE — there is nothing to bind here. Wrap it: \
                                  `|x| {{ x.{name}(…) }}`, or declare the function with a plain \
-                                 first-parameter name (not `self`), which makes it a free \
-                                 function and a usable fn-ref"
+                                 first-parameter name (not `self` / `both`), which makes it a \
+                                 free function and a usable fn-ref"
                             );
                         }
                     }
