diff --git b/src/generation/dispatch.rs a/src/generation/dispatch.rs
index c5532c7a..445e501d 100644
--- b/src/generation/dispatch.rs
+++ a/src/generation/dispatch.rs
@@ -861,14 +861,8 @@ impl Output<'_> {
             // returns its promoted local adopts the minted store: the plain assignment
             // below, the same emit a fresh-adopting callee's result takes.  A reassignment
             // keeps this arm's in-place copy, as the interpreter's reassignment path does.
-            // The local's one bind after its `if` pre-init is its first (`deferred_first_bind`),
-            // and so is a PREDECLARED local's first body `Set` — the prologue bound only the
-            // sentinel.  A pure chain `fn outer() -> Mk { scan() }` hoists the call into a
-            // returned `__ret_N` the prologue binds up front; read as a reassignment, its copy
-            // freed the callee's answer, which is the caller's pooled buffer (@PLN164 B1b).
-            && ((self.declared.contains(&var)
-                && !self.predeclared.contains(&var)
-                && !variables.is_deferred_first_bind(var))
+            // The local's one bind after its `if` pre-init is its first (`deferred_first_bind`).
+            && ((self.declared.contains(&var) && !variables.is_deferred_first_bind(var))
                 || !crate::use_analysis::adopts_minted_at_bind(self.data, variables, var, to))
         {
             let tp_nr = self.data.def(d_nr).known_type();
