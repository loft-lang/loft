// Copyright (c) 2026 Jurjen Stellingwerff
// SPDX-License-Identifier: LGPL-3.0-or-later
//
// A `struct` or `enum` declares no type variables: `<…>` after its name is refused once,
// where it is written, naming what to write instead.  It used to report three syntax errors
// at the `<` and nothing about what was meant.
// @falsified-at: 04f5791cf — exit 1 -> 0, expectations FAIL/2 -> 0/2
//   CHANNEL: expectations (the refusal is what moves; the program is refused on both trees).
// @EXPECT_ERROR: a struct declares no type variables — `Box<T>`; a container over any type is a generic FUNCTION over `vector<T>`
// @EXPECT_ERROR: an enum declares no type variables — `Shape<T>`; a container over any type is a generic FUNCTION over `vector<T>`

struct Box<T> { v: T }

enum Shape<T> { Dot { at: T }, Empty }

fn main() {
  println("ok");
}
