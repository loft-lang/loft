// Copyright (c) 2026 Jurjen Stellingwerff
// SPDX-License-Identifier: LGPL-3.0-or-later
//
// @PLN165 D3 — a template given another number of type arguments than its header declares
// is refused saying how many it takes; the refused type is poisoned, so nothing reports it
// again.
//
// @falsified-at: 684cd140f — interpret exit 1 -> 0, interpret expectations FAIL/1 -> 0/1 (the suite's verdict, not a count of matches), native exit 1 -> 0, native expectations FAIL/1 -> 0/1 (the suite's verdict, not a count of matches)
// @falsified-by: tests/falsified/a-generic-struct-takes-its-declared-number-of-arguments.patch — interpret exit 1 -> 0, interpret expectations FAIL/1 -> 0/1 (the suite's verdict, not a count of matches), native exit 1 -> 0, native expectations FAIL/1 -> 0/1 (the suite's verdict, not a count of matches)
//   CHANNEL: expectations — refused on both trees; the words move.
//   ARMED: nothing.
//   WITNESS: on the control `Box<integer, text>` is "`Box` is a generic struct — name its
//     type arguments".
//   HOLDS: the program is refused on both trees.
// @EXPECT_ERROR: `Box` takes 1 type argument, and 2 are given

struct Box<T> { v: T }
fn two(b: Box<integer, text>) -> integer { 1 }

fn main() {
  println("ok");
}
