{
  "M-close-plans": {
    "at": "2026-09-16 10:13",
    "note": "scripts/close-shipped-plans.sh --range v2026.9.0..HEAD --dry-run, 2026-09-16. Eight plan-close directives in range, naming loft-lang/plans issues 149 150 152 153 155 159 160 162, and ALL EIGHT report \"already closed, skipping\". Nothing shipped in this range left a plan open, which is the whole question the step asks. Deliberately run in dry-run only: the acting form calls gh issue close against a separate repo, and closing plan issues is the owner call, not the reviewer one. No outward action taken."
  },
  "M-falsify-receipts": {
    "at": "2026-09-16 09:10",
    "note": "make falsify-review run 2026-09-16 on 369f6de7e, and the --since v2026.9.0 read. Census: 486 falsification receipts; controls still publicly reachable 292, unreachable 194 (66 of those carry a patch); 106 patch receipts, 43 still applying and 63 no longer applying (listed by the report, to re-derive or downgrade to a marker). --since v2026.9.0 (24 commits) reads 194 controls unreachable NOW; this item has never run in any recorded cycle, so there is no previous line to subtract and this run ESTABLISHES the baseline the next cycle subtracts from to get the inflow rate. Scoring individual patch receipts with the instrument each names is the per-guard read and stays for the tag candidate."
  },
  "M-file-sizes": {
    "at": "2026-09-16 23:55",
    "note": "make file-sizes run 2026-09-16 at the row's creation; both halves read. SIZE (scripts/file-sizes.py): docs 1000 files, median 189 lines, 89 over the 700 bar, 36 reading as several subjects -- PERFORMANCE.md (32 sections, largest 17%), DESIGN_DECISIONS.md (70, 6%), TESTING.md (36, 20%), WASM.md (22, 13%), PLANNING.md (21, 22%), LOFT.md (29, 16%), PACKAGES.md (39, 22%), plans/157 DESIGN.md (56, 8%). Code 612 files, median 260, 96 over the 1200 bar, 45 as several subjects; the giant parser/state files are KEEP because one impl block is 74-97% of each (one subject, merely long), and the real candidate is src/use_analysis.rs (167 sections, largest 8%) -- tests/issues.rs (1550, 1%) is a test file where that shape is the point. HISTORY (scripts/doc_history_report.py): the split holds in general, 138 docs carrying 5155 timeline lines of 90793 (6%); it concentrates in the formal contract docs, where formal/heap.md is 71% history with NO -history.md companion (the only high-share contract doc without one) and binding 52%, tuples 59%, closures 49%, ownership 35% HAVE a companion and still carry that much -- a companion existing is not evidence the history moved, so the share is what to read. METHOD NOTE: the first version of file-sizes.py divided the largest section by the whole FILE while sections span first-anchor-to-EOF, so a doc with one late ## scored 2% and read as several subjects; 7 of 43 doc recommendations were false until a len(secs)<2 guard landed. WHAT REMAINS: no file was split. This tick records the census and the candidate list; each split is its own edit with its own review, and the heap.md history move is the one the record already scoped (RELEASE.md 5b: MOVE, never copy -- a register living in two files defines its entries twice)."
  },
  "M-libs": {
    "at": "2026-09-16 10:54",
    "note": "scripts/revalidate_libs_local.sh run 2026-09-16 on tuxedo-advisory-2026-09-16 (c5547719d), registry index 99862cf (2026-09-10, 0 behind origin/main, 43 packages). BUILDS ALL GREEN: 42 pass, 0 runtime/env, 0 skipped, 0 COMPILE-BREAK -- which is the question this row asks, since it exists to catch a language change that retro-breaks a shipped library, and nothing on this tree breaks one. The script nevertheless exits 1 on RELEASE-READINESS: two published libraries carry warnings and so fail their own LOFT_DENY_WARNINGS CI. graphics 0.9.1 twice, src/graphics.loft:1549:68 and :1581:53 -- const parameter atlas passed to plain parameter data of gl_upload_canvas, and const mvp to mat of gl_set_uniform_mat4 (C124, const-to-plain-parameter, which C124 section Rollout expects until the named libraries migrate). regex 0.3.1 once, src/regex.loft:86:55 -- both as a first parameter is deprecated (C123, both-receiver-deprecated, a WARNING by owner decision 2026-09-15 precisely so a library CI goes red and published uses get renamed). BOTH ARE PRE-EXISTING, measured not assumed: the loft2 peer ran the same published sources through the pre-fix binary at 6eece5cdd and the post-fix one and got byte-identical diagnostics, and both lints are parse-time declaration lints that no change on this branch can emit. Their cure is an edit in each library own repo, not here. WHAT REMAINS FOR THE TAG: those two must be republished warning-clean before the release claims them, which is an owner call and not this tree work."
  },
  "M-liveness": {
    "at": "2026-09-16 09:08",
    "note": "make release-liveness run 2026-09-16 on 369f6de7e. Reports: no issue-cited suppressions (so no stale rationales); gates whose LAST verdict is a failure = release-gate.yml (2026-09-08) and miri.yml (2026-09-16, the Debug-assertions and LOFT_VERIFY_STACK legs); 4 items never run in any recorded cycle (M-perf-pass, M-liveness, M-falsify-receipts, M-vscode). Acted on the miri red in the same session: the Debug-assertions leg was traced to place_result.rs asking the heap-return ownership question of a closure-returning callee (22 corpus files), fixed; the LOFT_VERIFY_STACK leg measures 0 of 1458 on this branch."
  },
  "M-monthly-bugs": {
    "at": "2026-09-16 10:13",
    "note": "make bug-review run 2026-09-16 over 697 bug issues (#246-#1544, 3 open). ONE rising class: null/sentinel, 12.1 to 4.8 to 18.9 to 25.9 percent across the four bands, +7.1pp against its own peak. Every other class is flat or falling (ownership/free -2.0, par/coroutine -4.5, narrow-int/width -4.5, wasm/browser -8.6, packages/registry -11.7). Payoff check: IntegerSpec::range_to_width PAID OFF, taking narrow-int/width 8.6 to 3.4 percent; four later keystones (Parser::nstore_unwrap_report, Scopes::owns_freeable_store, Function::proxy_says_owned, @FR-N-Road) landed too recently for any band to start above them, so re-run with ARGS=\"--bands 14\" next cycle rather than reading them as unproven. Enumeration exposure names the dangerous pairs: Tuple 76.3 percent omitted with 53 bugs, Parallel 78.1 with 30, Yield 76.9 with 30. Contract pressure: 606 unjudged against 392 judged, so the 15.9 percent strained ratio for 2026-09 is drawn from a minority and is not evidence either way yet. WHAT REMAINS: converting the rising class into ONE generalization is a design act and is not done here."
  },
  "M-monthly-docs": {
    "at": "2026-09-16 10:13",
    "note": "Both halves run 2026-09-16. make libraries-review: 8 reviewed, 41 never reviewed, 2 exempt, 1 deferred, 6 owe a verdict, 0 stale rows; 1441 published pub items with 36 undocumented (2 fn, 34 type); 359 in-tree pub fns. Five sources moved past their watermark (default, lib/code.loft, lib/lexer.loft, lib/parser.loft, lib/testlib.loft) and EACH reports 0 pub fn line(s) changed, so no public surface moved in any of them. make features-review: 71 entries with a runnable example, 14 exempt, 2 deferred (F47, F100), 0 unwritten, 0 owe a verdict, 35 infra entries needing no example. WHAT REMAINS: the by-hand read of those five prose deltas is NOT done. The census is what this tick records; the zero pub-fn-line finding is why the deltas are low risk, not why they are read."
  },
  "M-perf-pass": {
    "at": "2026-09-16 09:54",
    "note": "Both halves measured 2026-09-16 on host tuxedo. LOFT half: make speed (exit 0, quiet box), 87 tests timed and annotated, tolerance +/-25%, 17 slower / 20 faster. The three headline rows are STALE ANNOTATIONS, not regressions: features_examples_interpret is annotated @speed 3.5 (tests/features.rs:31) and measures 64.85 units here, which agrees with the 94.97 s I measured running that binary directly today; loft_suite is annotated @speed 5.8 (tests/wrap.rs:279) and the debug-assertions gate ran it at 131 s; native_scripts is annotated @speed 6.1 (tests/native.rs:962). All three are whole-corpus tests whose clock grows with the corpus, so the annotations went stale as the suite grew. NOT blessed: scripts/test_speed.py own header records that a re-run moved 48 of 134 annotated tests past the band with nothing changed, so one run is not evidence for rewriting the numbers into the tests -- that is an owner call (make speed-bless). LIBRARY half: already re-measured on this box today and recorded in formal/performance.md D-perf-1 -- nine of ten judged drawing-lock rows under the 4x (Perf-Weight) bar (hash 0.59x ... fronds 3.28x), smooth 4.25x on a 240 ns/op reference that sits under the clock floor and is therefore a noisy row rather than a measurement of the gap, and the four rows the bench grew since (parse, render_lock, render_marks, resize) carry no reference in that clone so they stay unjudged there and keep @PLN157s scoreboard numbers. D-perf-1 remains OPEN for those four rows and closes with @PLN157."
  }
}
