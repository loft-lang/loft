// @PLN150 cell tail/capture/depth0 — every value must be 13, and the capture must survive.
struct P { n: integer }
fn main() {
  cap = P { n: 13 };
  s = fn(v: integer) -> P { cap };
  out = "";
  for i in 0..4 { r = s(1); out = out + "{r.n} "; }
  println("{out}| cap {cap.n}");
}
