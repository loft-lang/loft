// @CONTROL — deliberately wrong expectation.
// @EXPECT: [9,9,9]
fn my_map<T>(v: vector<T>, f: fn(T) -> T) -> vector<T> {
  out: vector<T> = [];
  for x in v { out += [f(x)]; }
  out
}
fn main() {
  a: vector<integer> = [1, 2, 3];
  println("{my_map(a, |x| { x })}");
}
