// C2 — a tuple return built from both variables.
// @EXPECT: 7 q|r 8
fn both<A, B>(a: A, b: B) -> (A, B) { (a, b) }
fn main() {
  t = both(7, "q");
  u = both("r", 8);
  println("{t.0} {t.1}|{u.0} {u.1}");
}
