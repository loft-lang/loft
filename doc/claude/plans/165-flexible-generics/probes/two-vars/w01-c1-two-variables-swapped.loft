// C2 — c1: two variables bound to swapped types; two instances.
// @EXPECT: 1:a|b:2
fn pair_up<K: Printable, V: Printable>(k: K, v: V) -> text { "{k}:{v}" }
fn main() {
  println("{pair_up(1, "a")}|{pair_up("b", 2)}");
}
