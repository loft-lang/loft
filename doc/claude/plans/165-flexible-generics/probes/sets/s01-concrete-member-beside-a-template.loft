// B2 — a template and a concrete definition of one name are one set; the concrete member
// takes the call it declares.
// @EXPECT: 999
fn first<T>(v: vector<T>) -> T { v[0] }
fn first(v: vector<integer>) -> integer { 999 }
fn main() {
  a: vector<integer> = [1, 2, 3];
  println("{first(a)}");
}
