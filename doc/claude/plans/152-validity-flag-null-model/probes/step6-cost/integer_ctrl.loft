// CONTROL — plain `integer` keeps a sentinel, is never fused, and must not move.
fn main() {
  n = 20000000;
  acc: integer = 0;
  sink = 0;
  t0 = ticks();
  for i in 0..n {
    acc += i & 7;
    sink += acc;
  }
  ms = (ticks() - t0) / 1000;
  println("result: {sink}  time: {ms}ms");
}
