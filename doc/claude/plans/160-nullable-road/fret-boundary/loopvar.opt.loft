struct S { a: integer }
fn f(p: vector<S>) -> S? { for e in p { return e; } null }
fn bump(s: S) { s.a = s.a + 1; }
fn main() { q: vector<S> = [S { a: 7 }]; bump(f(q) ?? S { a: 0 }); println("{(f(q) ?? S { a: 0 }).a}"); }
